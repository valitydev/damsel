name: "Frontend: Publish"

on:
  push:
    branches: ["master", "main"]
  pull_request:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    uses: ./.github/workflows/proto-publish.yml
