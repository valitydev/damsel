name: "Frontend: Publish"

on:
  push:
    branches: ["master", "main"]

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    name: Publish
    runs-on: ubuntu-latest
    steps:
      - uses: valitydev/action-frontend/proto-publish@v3
